# langchain-mcp-adapters 0.1.7 requires langchain-core<0.4, so all langchain
# packages must stay on the 0.3.x series.
langchain>=0.3,<0.4
langchain-core>=0.3.36,<0.4
langchain-openai>=0.3,<0.4
langchain-mcp-adapters==0.1.7
langgraph>=0.2,<0.4

fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic>=2.0,<3

# MCP Python SDK (required by langchain-mcp-adapters)
mcp>=1.9.0

python-dotenv>=1.0

# wrapt locked to 1.x — AM's auto-instrumentation injects
# opentelemetry-instrumentation-langchain 0.62b0, which calls
# wrap_function_wrapper(module=..., name=..., wrapper=...). wrapt 2.x
# dropped that keyword argument — see traceloop/openllmetry#4009.
wrapt==1.17.3
